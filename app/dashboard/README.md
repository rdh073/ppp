# PPP Dashboard

Frontend dashboard for PPP (React + Vite).

## Run Dashboard

```bash
cd app/dashboard
npm install
npm run dev
```

To serve with default settings:

```bash
npm run dev
```

Other useful commands:

```bash
npm run build
npm run type-check
npm test
```

By default, Vite serves on `http://localhost:5173/`.
